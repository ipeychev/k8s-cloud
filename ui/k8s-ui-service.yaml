apiVersion: "v1"
kind: "Service"
metadata:
  name: "ui-service"
  namespace: "default"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 3005
  selector:
    app: "ui-1.0.4"
  type: "NodePort"
